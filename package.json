{
  "name": "monatise-ui-quality",
  "private": true,
  "scripts": {
    "cloudflare:check": "tsc -p tsconfig.cloudflare.json && wrangler deploy --dry-run",
    "cloudflare:deploy": "wrangler deploy",
    "test:visual": "playwright test",
    "test:lighthouse": "lhci autorun"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "@playwright/test": "^1.54.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.120.0"
  }
}
